const RateType = Object.freeze({
    MASS: 'MASS',
    SIZE: 'SIZE',
    LDM: 'LDM'
})

const ShippingType = Object.freeze({
    TRAIN: "TRAIN",
    AIR: "AIR",
    TRUCK: "TRUCK"
})

export {RateType, ShippingType}

